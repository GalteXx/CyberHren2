#include "base.h"

int* eat(string a)
{
	int whatAstroyBob = 0;
	int ThisIsTheLongestVarriableTypedIntegerNameIHaveEverWrittenInMyEntireLifeLmao = 0;
	for(int i = 0; a[i] != '\0' || a[n] != '\n'; i++)
	{
			if(a[i] == ' ' && whatAstroyBob != 0)
			{
				a[ThisIsTheLongestVarriableTypedIntegerNameIHaveEverWrittenInMyEntireLifeLmao] = whatAstroyBob;
				ThisIsTheLongestVarriableTypedIntegerNameIHaveEverWrittenInMyEntireLifeLmao++;
			}
			else if(isNumeric(a[i]))
	}
}
